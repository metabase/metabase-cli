#!/usr/bin/env bash
set -euo pipefail
ROOT=$(cd "$(dirname "$0")/.." && pwd)
export XDG_CONFIG_HOME="$ROOT/.dev-state"
export METABASE_CLI_DISABLE_KEYRING=1
mkdir -p "$XDG_CONFIG_HOME"
exec bun "$ROOT/src/cli.ts" "$@"
