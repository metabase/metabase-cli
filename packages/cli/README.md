# @metabase/cli

Command-line client for Metabase — the `mb` binary.

```sh
npm install -g @metabase/cli
mb --help
```

Commands, authentication, output flags and environment variables are documented at
<https://github.com/metabase/mb-cli#readme>.
